[pytest]
testpaths = tests
addopts = -q
# The app is a plain package in the repo root, not an installed distribution, so
# `import app` only resolves if the root is on sys.path. `python -m pytest` adds
# it implicitly, the bare `pytest` script does not; this makes both work.
pythonpath = .
